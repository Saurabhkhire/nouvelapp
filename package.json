{
  "name": "nouvel",
  "version": "1.0.0",
  "private": true,
  "description": "Nouvel — run frontend + backend together",
  "scripts": {
    "setup": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -k -n BACKEND,FRONTEND -c magenta,cyan \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "start": "npm run dev",
    "test": "npm --prefix backend test",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
